defaults:
  model:
    api_base: https://api.deepseek.com/v1
    name: deepseek-chat
    provider: deepseek
  catch_up:
    mode: queue
    max_missed: 5
    ttl_minutes: 120
health:
  notify_on_missed_cron: true
overrides: {}
watchdog:
  interval_s: 60
